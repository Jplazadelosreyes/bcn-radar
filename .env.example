# Credenciales TMB — SOLO para descargar el GTFS estático una vez.
# NO se usan en runtime ni se incluyen en el build (sin prefijo VITE_).
# Copia este archivo a .env y rellena los valores (developer.tmb.cat).
TMB_APP_ID=
TMB_APP_KEY=
